# POC handoff package — runtime dependencies
#
# Core LLM SDKs (the simulator uses Anthropic; the actor uses Gemini)
anthropic>=0.40.0
google-genai>=0.1.0

# HTTP for the simulator's embedding fallback + the strategist KG client
httpx>=0.25.0

# Dual-panel UI server (server/main.py)
fastapi>=0.110.0
uvicorn>=0.27.0

# Data
pyyaml>=6.0
numpy>=1.24
pydantic>=2.0

# Optional dev/test
pytest>=7.0
python-dotenv>=1.0
